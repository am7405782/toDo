import 'package:flutter/material.dart';

class Seting_Screen extends StatelessWidget {
  const Seting_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
